/** Field types hidden from data table columns (complex/nested types). */
export const EXCLUDED_TYPES = [
  "component",
  "dynamiczone",
  "relation",
  "richtext",
  "text",
  "json",
];

export const DEFAULT_VISIBLE_COUNT = 5;
